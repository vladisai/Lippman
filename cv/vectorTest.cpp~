#include <bits/stdc++.h>
#include "customVector.cpp"

#define F first
#define S second
#define pb push_back
#define INF (1 << 30)
#define SQR(a) ((a) * (a))

using namespace std;

int main()
{
	int t = clock();

	Vector<int> v{1, 2, 3, 4};

	int tf = clock() - t;
	cout << static_cast<double>(tf) / CLOCKS_PER_SEC;
	return 0;
}
